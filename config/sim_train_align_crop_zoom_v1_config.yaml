# Crop & Zoom-in v1 (2026-05-20) — Phase 2 alignment 전용 trocar UV 주변 crop
# Hypothesis: SigLIP2 patch16-512 + 512x512에서 5mm 거리 trocar(1.8mm) = 2-3 pixel,
# patch16 내부 sub-pixel scale → 모델이 0.2mm 좌/우 식별 불가능 (해상도 한계).
# Solution: alignment data만 trocar UV 주변 256x256 crop → 512 resize (2× zoom).
# Approach data는 wide view 유지 (멀리서 trocar 보일 때 wide 유리).
#
# Base: champion v3 ckpt 1000 (fresh start, crop axis 단독 효과 측정)
# extreme_rebal_v1 mix 동일 + per-path crop flag 추가
#
# Target: close_2mm 3.7% → 10%+ OR close_1mm > 0% (천장 첫 돌파)

project:
  name: "VLANeXt_SigLIP2_NEARGOAL_crop_zoom_v1"
  output_dir: "/home/najo/NAS/VLANeXt/checkpoints"
  log_interval: 1
  save_interval: 500   # sparse eval (250/500/1000/1500/final 중 500 단위)
  use_wandb: true

data:
  dataset_name: "sim_align"
  # Crop & Zoom-in: alignment data paths만 crop_around_trocar=true.
  # trocar UV 주변 256x256 raw crop → 512x512 resize → 2× zoom 효과.
  # SigLIP2 patch16-512에서 trocar effective pixel 면적 4배 증가 (sub-pixel 한계 완화).
  crop_window_px: 256
  crop_target_size_px: 512
  data_root:
  # ===== Approach paths: NO crop (wide view 유지) =====
  - path: "/home/najo/NAS/VLANeXt/dataset/approach/approach_00"
    max_episodes: 2000   # extreme_rebal과 동일 spec (cap 2K)
  - path: "/home/najo/NAS/VLANeXt/dataset/approach/approach_eval_range_v1"
    max_episodes: 1000
  # ===== Alignment paths: crop_around_trocar=true =====
  - path: "/home/najo/NAS/VLANeXt/dataset/fine_align/10mm_fine_align_00_tip2/collected_data_merged"
    crop_around_trocar: true
  - path: "/home/najo/NAS/VLANeXt/dataset/fine_align/align_phantom_range_v1/collected_data_merged"
    max_episodes: 200
    crop_around_trocar: true
  - path: "/home/najo/NAS/VLANeXt/dataset/fine_align/NEARGOAL_eval_match_v2/collected_data_merged"
    crop_around_trocar: true
  - path: "/home/najo/NAS/VLANeXt/dataset/fine_align/NEARGOAL_angle_only_v2/collected_data_merged"
    crop_around_trocar: true

  task_suite_name: ""
  full_sequence: true
  skip_history_padding: true

  history_len: 8
  future_len: 8
  buffer_size: 4000

  batch_size: 16
  max_steps: 2000      # 짧게 — champion ckpt1000 baseline, finetune 2K 이내에 효과 확인
  num_workers: 4

  input_modality: "image"
  view_mode: "single"

  cam_exterior: "tool_camera"
  cam_wrist: ""
  cam_top: ""

  augmentation:
    enabled: false

model:
  model_type: "vlanext"
  lmm_path: "vision_only"
  vision_encoder_path: "google/siglip2-so400m-patch16-512"
  backbone_mode: "frozen"
  lora:
    r: 16
    lora_alpha: 32
    lora_dropout: 0.05
    target_modules: ["q_proj", "v_proj", "k_proj", "out_proj"]

  action_dim: 6
  proprio_dim: 6
  use_sensor: false

  loss_type: "diffusion"
  num_train_timesteps: 1000
  num_inference_timesteps: 10
  scheduler_type: "flow_match"
  num_bins: 256
  action_vqvae:
    enabled: false
    frozen: true
    codebook_size: 1024
    hidden_size: 256
    depth: 4
    num_heads: 8
    steps: 1000
    learning_rate: 1.0e-3

  condition_type: "soft"
  num_queries: 32
  use_transformer_connector: true
  connector_depth: 2
  connector_num_heads: 4

  policy_hidden_size: 1152
  policy_depth: 24
  policy_num_heads: 16
  policy_mlp_ratio: 4.0

  future_image_loss_weight: 0.0
  generator_hidden_size: 768
  generator_depth: 24
  generator_num_heads: 12
  generator_mlp_ratio: 4.0
  future_image_mode: "horizon"

  dct_loss_weight: 0.1
  dct_freq_split: 0.125
  dct_low_freq_weight: 1.0
  dct_high_freq_weight: 1.0
  dct_similarity_type: "mae"

  use_proprio_input_vlm: true
  use_transformer_proprio_projector: false
  projector_depth: 2
  projector_num_heads: 4
  use_action_input_policy: false
  gradient_checkpointing: true

  aux_distance_loss:
    enabled: true
    weight: 0.5
    margin_mm: 0.1
    pos_denorm_mm: [0.37, 0.37, 0.37]
    near_goal_scale_mm: 2.0
    near_goal_max_boost: 10.0
    apply_to_main_loss: true

  direction_decoupled_loss:
    enabled: false

train:
  seed: 2026
  resume_path: ""
  # CRITICAL: champion v3 ckpt 1000 base (best so far)
  pretrained_checkpoint: "/home/najo/NAS/VLANeXt/checkpoints/VLANeXt_SigLIP2_repro/b24_ft10mm_aux_strong_v3/checkpoint_1000.pt"
  reset_optimizer_scheduler: true
  learning_rate: 1.0e-6
  weight_decay: 0.01
  warmup_steps: 100               # short finetune이라 warmup 짧게
  gradient_accumulation_steps: 1
  max_grad_norm: 1.0
  device: "cuda"
  distributed: false
  dist_backend: "nccl"
  deepspeed:
    enabled: false
    zero_stage: 2
    offload_optimizer_device: "none"
    offload_param_device: "none"
